import React from 'react';
import '../Spinner/Spinner.css'

const Spinner = () => {
    return (
        <div class="loader">
        <span>Loading...</span>
        </div>
    );
};

export default Spinner;